requestId: {{requestId}}
userId: {{userId}}
periodDays: {{periodDays}}
userMessage: {{userMessage}}

Инструкция:
- Если доступны tools, сначала получи факты из tools по userId и periodDays.
- Если tools недоступны или фактов мало, не выдумывай суммы и метрики; явно напиши, каких данных не хватает.
- Верни строго JSON:
{"summary":"...","advice":"..."}